name: Release_tags
on:
  push:
    tags:
      - '*'

env:
  service_name: expense-frontend


jobs:
  CI_Steps:
    uses: Sdevops-prac/github-actions/.github/workflows/Release_Tags.yml@main
    secrets: inherit
    with:
      service_name: expense-frontend
